image: Ubuntu  # образ для сборки

stack: jdk 11  # версия JDK

branches:
  only:
    - main  # ветка git

build: Script  # будем использовать свой скрипт сборки

install:

  -

build_script:
  - chmod +x gradlew
  - ./gradlew test --info